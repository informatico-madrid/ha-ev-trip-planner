{
  "classes_found": 30,
  "review_needed": true,
  "review_context": "======================================================================\nSOLID PRINCIPLE REVIEW - CLASS INVENTORY\n======================================================================\n\nTotal classes found: 30\n\n--------------------------------------------------\nClass #1: EMHASSAdapter\nFile: custom_components/ev_trip_planner/emhass_adapter.py:60\nBase classes: None\nPublic methods: 27\n\n  [private] __init__(arity=3, NO-return)\n  [PUBLIC] async_load(arity=1, NO-return)\n  [private] _get_coordinator(arity=1, +return)\n  [PUBLIC] get_cached_optimization_results(arity=1, +return)\n  [PUBLIC] async_save(arity=1, NO-return)\n  [PUBLIC] async_assign_index_to_trip(arity=2, +return)\n  [PUBLIC] async_release_trip_index(arity=2, +return)\n  [private] _get_config_sensor_id(arity=2, +return)\n  [PUBLIC] async_publish_deferrable_load(arity=2, +return)\n  [private] _calculate_deadline_from_trip(arity=2, +return)\n  [private] _populate_per_trip_cache_entry(arity=12, +return)\n  [PUBLIC] async_remove_deferrable_load(arity=2, +return)\n  [PUBLIC] async_update_deferrable_load(arity=2, +return)\n  [PUBLIC] async_publish_all_deferrable_loads(arity=4, +return)\n  [PUBLIC] get_assigned_index(arity=2, +return)\n  [PUBLIC] get_all_assigned_indices(arity=1, +return)\n  [PUBLIC] get_available_indices(arity=1, +return)\n  [PUBLIC] calculate_deferrable_parameters(arity=3, +return)\n  [PUBLIC] publish_deferrable_loads(arity=3, +return)\n  [PUBLIC] async_verify_shell_command_integration(arity=1, +return)\n  [PUBLIC] async_check_emhass_response_sensors(arity=2, +return)\n  [PUBLIC] async_get_integration_status(arity=1, +return)\n  [PUBLIC] async_notify_error(arity=4, +return)\n  [private] _async_update_error_status(arity=4, +return)\n  [private] _async_send_error_notification(arity=4, +return)\n  [private] _async_call_notification_service(arity=3, +return)\n  [PUBLIC] async_handle_emhass_unavailable(arity=3, +return)\n  [PUBLIC] async_handle_sensor_error(arity=4, +return)\n  [PUBLIC] async_handle_shell_command_failure(arity=2, +return)\n  [PUBLIC] get_last_error(arity=1, +return)\n  [PUBLIC] async_clear_error(arity=1, +return)\n  [PUBLIC] async_cleanup_vehicle_indices(arity=1, +return)\n  [PUBLIC] verify_cleanup(arity=1, +return)\n  [PUBLIC] setup_config_entry_listener(arity=1, +return)\n  [private] _handle_config_entry_update(arity=3, +return)\n  [PUBLIC] update_charging_power(arity=2, +return)\n  [private] _calculate_power_profile_from_trips(arity=7, +return)\n  [private] _generate_schedule_from_trips(arity=3, +return)\n  [private] _get_current_soc(arity=1, +return)\n  [private] _get_hora_regreso(arity=1, +return)\n\nSource preview:\nclass EMHASSAdapter:\n    \"\"\"Adapter to publish trips as EMHASS deferrable loads.\"\"\"\n\n    def __init__(self, hass: HomeAssistant, entry: ConfigEntry):\n        \"\"\"Initialize adapter.\"\"\"\n        self.hass = hass\n        # Always store entry (for _get_current_soc access to soc_sensor)\n        self._entry = entry\n\n        # Handle both dict (backward compatibility for tests) and ConfigEntry\n        if isinstance(entry, dict):\n            self.entry_id = entry.get(\"vehicle_name\", \"unknown\")\n            self._entry_dict: Any = entry  # Keep dict for _get_current_soc\n            entry_data: Any = entry\n        elif hasattr(entry, \"data\"):\n            # ConfigEntry or MockConfigEntry with data attribute\n            self.entry_id = entry.entry_id\n            self._entry_dict = entry.data  # Keep dic\n\n--------------------------------------------------\nClass #2: PresenceMonitor\nFile: custom_components/ev_trip_planner/presence_monitor.py:37\nBase classes: None\nPublic methods: 11\n\n  [private] __init__(arity=5, NO-return)\n  [PUBLIC] async_check_home_status(arity=1, +return)\n  [PUBLIC] async_check_plugged_status(arity=1, +return)\n  [PUBLIC] async_handle_return_home(arity=2, +return)\n  [PUBLIC] async_get_hora_regreso(arity=1, +return)\n  [private] _async_persist_return_info(arity=1, +return)\n  [PUBLIC] async_check_charging_readiness(arity=1, +return)\n  [private] _async_check_home_sensor(arity=1, +return)\n  [private] _async_check_home_coordinates(arity=1, +return)\n  [private] _parse_coordinates(arity=2, +return)\n  [private] _calculate_distance(arity=3, +return)\n  [private] _async_setup_soc_listener(arity=1, +return)\n  [private] _async_handle_soc_change(arity=2, +return)\n  [PUBLIC] async_notify_charging_not_possible(arity=3, +return)\n  [PUBLIC] async_notify_vehicle_not_home(arity=2, +return)\n  [PUBLIC] async_notify_vehicle_not_plugged(arity=2, +return)\n  [private] _async_send_notification(arity=3, +return)\n  [PUBLIC] get_home_condition_config(arity=1, +return)\n  [PUBLIC] get_plugged_condition_config(arity=1, +return)\n  [PUBLIC] validate_condition_is_native(arity=2, +return)\n\nSource preview:\nclass PresenceMonitor:\n    \"\"\"Monitors vehicle presence and charging status.\"\"\"\n\n    def __init__(\n        self,\n        hass: HomeAssistant,\n        vehicle_id: str,\n        config: Dict[str, Any],\n        trip_manager: Optional[\"TripManager\"] = None,\n    ):\n        \"\"\"Initialize presence monitor.\"\"\"\n        self.hass = hass\n        self.vehicle_id = vehicle_id\n        self._trip_manager = trip_manager\n\n        # Sensor-based detection (priority 1)\n        self.home_sensor = config.get(CONF_HOME_SENSOR)\n        self.plugged_sensor = config.get(CONF_PLUGGED_SENSOR)\n\n        # SOC sensor for state of charge tracking\n        self.soc_sensor = config.get(CONF_SOC_SENSOR)\n\n        # Coordinate-based detection (priority 2)\n        self.home_coords = self._parse_coordinates(\n            config.g\n\n--------------------------------------------------\nClass #3: DashboardError\nFile: custom_components/ev_trip_planner/dashboard.py:121\nBase classes: Exception\nPublic methods: 0\n\n  [private] __init__(arity=3, +return)\n\nSource preview:\nclass DashboardError(Exception):\n    \"\"\"Base exception for dashboard-related errors.\"\"\"\n\n    def __init__(self, message: str, details: Optional[dict[str, Any]] = None) -> None:\n        \"\"\"Initialize the dashboard error.\n\n        Args:\n            message: Human-readable error message.\n            details: Optional additional error details (e.g., file paths, config).\n        \"\"\"\n        super().__init__(message)\n        self.message = message\n        self.details = details or {}\n\n--------------------------------------------------\nClass #4: DashboardNotFoundError\nFile: custom_components/ev_trip_planner/dashboard.py:136\nBase classes: DashboardError\nPublic methods: 0\n\n  [private] __init__(arity=3, +return)\n\nSource preview:\nclass DashboardNotFoundError(DashboardError):\n    \"\"\"Raised when a dashboard template is not found.\"\"\"\n\n    def __init__(self, template_file: str, searched_paths: list[str]) -> None:\n        \"\"\"Initialize the dashboard not found error.\n\n        Args:\n            template_file: The template file that was not found.\n            searched_paths: List of paths that were searched.\n        \"\"\"\n        message = f\"Dashboard template not found: {template_file}\"\n        super().__init__(\n            message,\n            {\n                \"template_file\": template_file,\n                \"searched_paths\": searched_paths,\n                \"error_type\": \"template_not_found\",\n            },\n        )\n\n--------------------------------------------------\nClass #5: DashboardValidationError\nFile: custom_components/ev_trip_planner/dashboard.py:157\nBase classes: DashboardError\nPublic methods: 0\n\n  [private] __init__(arity=3, +return)\n\nSource preview:\nclass DashboardValidationError(DashboardError):\n    \"\"\"Raised when a dashboard configuration fails validation.\"\"\"\n\n    def __init__(self, error_type: str, validation_message: str) -> None:\n        \"\"\"Initialize the dashboard validation error.\n\n        Args:\n            error_type: The type of validation error.\n            validation_message: The validation error message.\n        \"\"\"\n        message = f\"Dashboard validation failed: {validation_message}\"\n        super().__init__(\n            message,\n            {\n                \"error_type\": error_type,\n                \"validation_message\": validation_message,\n            },\n        )\n\n--------------------------------------------------\nClass #6: DashboardStorageError\nFile: custom_components/ev_trip_planner/dashboard.py:177\nBase classes: DashboardError\nPublic methods: 0\n\n  [private] __init__(arity=3, +return)\n\nSource preview:\nclass DashboardStorageError(DashboardError):\n    \"\"\"Raised when dashboard storage fails.\"\"\"\n\n    def __init__(self, storage_method: str, error: str) -> None:\n        \"\"\"Initialize the dashboard storage error.\n\n        Args:\n            storage_method: The storage method that failed.\n            error: The storage error message.\n        \"\"\"\n        message = f\"Dashboard storage failed for {storage_method}: {error}\"\n        super().__init__(\n            message,\n            {\n                \"storage_method\": storage_method,\n                \"error\": error,\n                \"error_type\": \"storage_error\",\n            },\n        )\n\n--------------------------------------------------\nClass #7: DashboardImportResult\nFile: custom_components/ev_trip_planner/dashboard.py:198\nBase classes: None\nPublic methods: 1\n\n  [private] __init__(arity=8, +return)\n  [PUBLIC] to_dict(arity=1, +return)\n  [private] __str__(arity=1, +return)\n\nSource preview:\nclass DashboardImportResult:\n    \"\"\"Structured result from dashboard import operations.\n\n    Provides detailed error information for troubleshooting dashboard deployment\n    failures without requiring log inspection.\n    \"\"\"\n\n    def __init__(\n        self,\n        success: bool,\n        vehicle_id: str,\n        vehicle_name: str,\n        error: Optional[str] = None,\n        error_details: Optional[dict[str, Any]] = None,\n        dashboard_type: str = \"simple\",\n        storage_method: str = \"unknown\",\n    ) -> None:\n        \"\"\"Initialize the dashboard import result.\n\n        Args:\n            success: Whether the dashboard import was successful.\n            vehicle_id: The vehicle ID.\n            vehicle_name: The vehicle name.\n            error: Error message if import failed.\n           \n\n--------------------------------------------------\nClass #8: BatteryCapacity\nFile: custom_components/ev_trip_planner/calculations.py:54\nBase classes: None\nPublic methods: 1\n\n  [private] _compute_capacity(arity=1, +return)\n  [PUBLIC] get_capacity(arity=2, +return)\n  [private] _read_soh(arity=2, +return)\n\nSource preview:\nclass BatteryCapacity:\n    \"\"\"Abstraction for real battery capacity with SOH-aware fallback.\n\n    Wraps nominal capacity and optional SOH sensor lookup to compute\n    the effective (degraded) capacity used everywhere in the system.\n\n    Attributes:\n        nominal_capacity_kwh: Battery's rated (nominal) capacity in kWh.\n        soh_sensor_entity_id: Optional HA sensor entity for SOH (%).\n    \"\"\"\n\n    nominal_capacity_kwh: float\n    soh_sensor_entity_id: Optional[str] = None\n    _soh_value: Optional[float] = None\n    _soh_cached_at: Optional[datetime] = None\n    SOH_CACHE_TTL_SECONDS: int = SOH_CACHE_TTL_SECONDS  # type: ignore[misc]\n\n    def _compute_capacity(self) -> float:\n        \"\"\"Compute real capacity from nominal + cached SOH value.\"\"\"\n        if self._soh_value is not None:\n       \n\n--------------------------------------------------\nClass #9: ChargingDecision\nFile: custom_components/ev_trip_planner/calculations.py:364\nBase classes: None\nPublic methods: 0\n\n\nSource preview:\nclass ChargingDecision:\n    \"\"\"Immutable charging decision for a single trip.\n\n    Encapsulates the decision logic for whether and how much to charge,\n    extracted from EMHASSAdapter._populate_per_trip_cache_entry for SOLID SRP.\n    \"\"\"\n\n    trip_id: str\n    kwh_needed: float  # Energy to charge (0 = no charge needed)\n    def_total_hours: int  # Hours of charging needed\n    power_watts: float  # Charging power (0 = no charge)\n    needs_charging: bool  # Whether charging is needed\n\n--------------------------------------------------\nClass #10: TripPlannerSensor\nFile: custom_components/ev_trip_planner/sensor.py:81\nBase classes: RestoreSensor, SensorEntity\nPublic methods: 4\n\n  [private] __init__(arity=4, +return)\n  [PUBLIC] async_added_to_hass(arity=1, +return)\n  [PUBLIC] native_value(arity=1, +return)\n  [PUBLIC] extra_state_attributes(arity=1, +return)\n  [PUBLIC] device_info(arity=1, +return)\n\nSource preview:\nclass TripPlannerSensor(\n    CoordinatorEntity[TripPlannerCoordinator], RestoreSensor, SensorEntity\n):\n    \"\"\"Sensor base for EV Trip Planner using CoordinatorEntity pattern.\n\n    Reads from coordinator.data via entity_description.value_fn().\n    Sets _attr_unique_id = f\"{DOMAIN}_{vehicle_id}_{description.key}\".\n    Inherits RestoreSensor for state restoration on HA restart.\n    \"\"\"\n\n    def __init__(\n        self,\n        coordinator: TripPlannerCoordinator,\n        vehicle_id: str,\n        entity_description: TripSensorEntityDescription,\n    ) -> None:\n        \"\"\"Initialize the sensor.\n\n        Args:\n            coordinator: TripPlannerCoordinator instance.\n            vehicle_id: Vehicle identifier.\n            entity_description: Description with value_fn and attrs_fn.\n        \"\"\"\n    \n\n--------------------------------------------------\nClass #11: EmhassDeferrableLoadSensor\nFile: custom_components/ev_trip_planner/sensor.py:158\nBase classes: SensorEntity\nPublic methods: 4\n\n  [private] __init__(arity=3, +return)\n  [PUBLIC] native_value(arity=1, +return)\n  [PUBLIC] extra_state_attributes(arity=1, +return)\n  [PUBLIC] device_info(arity=1, +return)\n  [PUBLIC] async_will_remove_from_hass(arity=1, +return)\n\nSource preview:\nclass EmhassDeferrableLoadSensor(\n    CoordinatorEntity[TripPlannerCoordinator], SensorEntity\n):\n    \"\"\"Sensor para el perfil de carga diferible de EMHASS.\n\n    Este sensor proporciona los datos necesarios para la integraci\u00f3n con EMHASS:\n    - power_profile_watts: Array de potencia en watts por hora\n    - deferrables_schedule: Calendario de cargas diferibles\n\n    PHASE 3: Ahora hereda de CoordinatorEntity y lee desde coordinator.data.\n\n    Platform: template\n    Entity: sensor.emhass_perfil_diferible_{entry_id}\n    \"\"\"\n\n    def __init__(\n        self,\n        coordinator: TripPlannerCoordinator,\n        entry_id: str,\n    ) -> None:\n        \"\"\"Inicializa el sensor de carga diferible.\n\n        Args:\n            coordinator: TripPlannerCoordinator instance.\n            entry_id: Config entry\n\n--------------------------------------------------\nClass #12: TripSensor\nFile: custom_components/ev_trip_planner/sensor.py:377\nBase classes: SensorEntity\nPublic methods: 3\n\n  [private] __init__(arity=4, +return)\n  [private] _get_trip_data(arity=1, +return)\n  [PUBLIC] native_value(arity=1, +return)\n  [PUBLIC] extra_state_attributes(arity=1, +return)\n  [PUBLIC] device_info(arity=1, +return)\n\nSource preview:\nclass TripSensor(CoordinatorEntity[TripPlannerCoordinator], SensorEntity):\n    \"\"\"Sensor for a specific trip using CoordinatorEntity pattern.\n\n    Reads trip data from coordinator.data[\"recurring_trips\"][trip_id] or\n    coordinator.data[\"punctual_trips\"][trip_id].\n\n    Entity: sensor.ev_trip_planner_{vehicle_id}_trip_{trip_id}\n    \"\"\"\n\n    def __init__(\n        self,\n        coordinator: TripPlannerCoordinator,\n        vehicle_id: str,\n        trip_id: str,\n    ) -> None:\n        \"\"\"Initialize the trip sensor.\n\n        Args:\n            coordinator: TripPlannerCoordinator instance.\n            vehicle_id: Vehicle identifier.\n            trip_id: Trip identifier.\n        \"\"\"\n        super().__init__(coordinator)\n        self.coordinator = coordinator\n        self._vehicle_id = vehicle_id\n  \n\n--------------------------------------------------\nClass #13: TripEmhassSensor\nFile: custom_components/ev_trip_planner/sensor.py:933\nBase classes: SensorEntity\nPublic methods: 3\n\n  [private] __init__(arity=4, +return)\n  [PUBLIC] native_value(arity=1, +return)\n  [PUBLIC] extra_state_attributes(arity=1, +return)\n  [private] _zeroed_attributes(arity=1, +return)\n  [PUBLIC] device_info(arity=1, +return)\n\nSource preview:\nclass TripEmhassSensor(CoordinatorEntity[TripPlannerCoordinator], SensorEntity):\n    \"\"\"Sensor for per-trip EMHASS parameters.\n\n    This is a new sensor class for PHASE 4, separate from existing trip sensors.\n    It reads emhass_index from coordinator.data[\"per_trip_emhass_params\"].\n\n    Attributes:\n        native_value: The emhass_index for the trip, or -1 if not found\n    \"\"\"\n\n    def __init__(\n        self,\n        coordinator: TripPlannerCoordinator,\n        vehicle_id: str,\n        trip_id: str,\n    ) -> None:\n        \"\"\"Initialize the sensor.\n\n        Args:\n            coordinator: TripPlannerCoordinator instance.\n            vehicle_id: Vehicle identifier.\n            trip_id: Trip identifier.\n        \"\"\"\n        super().__init__(coordinator)\n        self.coordinator = coordinator\n \n\n--------------------------------------------------\nClass #14: CargaVentana\nFile: custom_components/ev_trip_planner/trip_manager.py:64\nBase classes: TypedDict\nPublic methods: 0\n\n\nSource preview:\nclass CargaVentana(TypedDict):\n    \"\"\"Structure for charging window information.\"\"\"\n\n    ventana_horas: float\n    kwh_necesarios: float\n    horas_carga_necesarias: float\n    inicio_ventana: Optional[datetime]\n    fin_ventana: Optional[datetime]\n    es_suficiente: bool\n\n--------------------------------------------------\nClass #15: SOCMilestoneResult\nFile: custom_components/ev_trip_planner/trip_manager.py:75\nBase classes: TypedDict\nPublic methods: 0\n\n\nSource preview:\nclass SOCMilestoneResult(TypedDict):\n    \"\"\"Return structure for calcular_hitos_soc function.\n\n    Contains SOC milestone calculation results for a single trip,\n    including the target SOC, energy requirements, accumulated deficit\n    from backward propagation, and charging window details.\n    \"\"\"\n\n    trip_id: str\n    soc_objetivo: float\n    kwh_necesarios: float\n    deficit_acumulado: float\n    ventana_carga: CargaVentana\n\n--------------------------------------------------\nClass #16: TripManager\nFile: custom_components/ev_trip_planner/trip_manager.py:90\nBase classes: None\nPublic methods: 31\n\n  [private] __init__(arity=7, +return)\n  [PUBLIC] set_emhass_adapter(arity=2, +return)\n  [PUBLIC] get_emhass_adapter(arity=1, +return)\n  [private] _validate_hora(arity=1, +return)\n  [private] _parse_trip_datetime(arity=3, +return)\n  [private] _sanitize_recurring_trips(arity=2, +return)\n  [PUBLIC] publish_deferrable_loads(arity=2, +return)\n  [PUBLIC] async_setup(arity=1, +return)\n  [private] _load_trips(arity=1, +return)\n  [private] _load_trips_yaml(arity=2, +return)\n  [private] _reset_trips(arity=1, +return)\n  [PUBLIC] async_save_trips(arity=1, +return)\n  [private] _save_trips_yaml(arity=2, +return)\n  [PUBLIC] async_get_recurring_trips(arity=1, +return)\n  [PUBLIC] async_get_punctual_trips(arity=1, +return)\n  [PUBLIC] get_all_trips(arity=1, +return)\n  [PUBLIC] async_add_recurring_trip(arity=1, +return)\n  [PUBLIC] async_add_punctual_trip(arity=1, +return)\n  [PUBLIC] async_update_trip(arity=3, +return)\n  [PUBLIC] async_delete_trip(arity=2, +return)\n  [PUBLIC] async_delete_all_trips(arity=1, +return)\n  [PUBLIC] async_pause_recurring_trip(arity=2, +return)\n  [PUBLIC] async_resume_recurring_trip(arity=2, +return)\n  [PUBLIC] async_update_trip_sensor(arity=2, +return)\n  [PUBLIC] async_complete_punctual_trip(arity=2, +return)\n  [PUBLIC] async_cancel_punctual_trip(arity=2, +return)\n  [private] _async_sync_trip_to_emhass(arity=4, +return)\n  [private] _async_remove_trip_from_emhass(arity=2, +return)\n  [private] _async_publish_new_trip_to_emhass(arity=2, +return)\n  [private] _get_all_active_trips(arity=1, +return)\n  [PUBLIC] async_get_kwh_needed_today(arity=1, +return)\n  [PUBLIC] async_get_hours_needed_today(arity=1, +return)\n  [private] _get_charging_power(arity=1, +return)\n  [PUBLIC] get_charging_power(arity=1, +return)\n  [private] _calcular_tasa_carga_soc(arity=3, +return)\n  [private] _calcular_soc_objetivo_base(arity=4, +return)\n  [PUBLIC] async_get_next_trip(arity=1, +return)\n  [PUBLIC] async_get_next_trip_after(arity=2, +return)\n  [private] _is_trip_today(arity=3, +return)\n  [private] _get_trip_time(arity=2, +return)\n  [private] _get_day_index(arity=2, +return)\n  [PUBLIC] async_get_vehicle_soc(arity=2, +return)\n  [PUBLIC] async_calcular_energia_necesaria(arity=3, +return)\n  [PUBLIC] calcular_ventana_carga(arity=6, +return)\n  [PUBLIC] calcular_ventana_carga_multitrip(arity=6, +return)\n  [PUBLIC] calcular_soc_inicio_trips(arity=7, +return)\n  [PUBLIC] calcular_hitos_soc(arity=6, +return)\n  [PUBLIC] async_generate_power_profile(arity=5, +return)\n  [PUBLIC] async_generate_deferrables_schedule(arity=3, +return)\n\nSource preview:\nclass TripManager:\n    \"\"\"Gesti\u00f3n central de viajes y optimizaci\u00f3n de carga para veh\u00edculos el\u00e9ctricos.\n\n    Esta clase implementa la l\u00f3gica de planificaci\u00f3n de viajes, c\u00e1lculo de energ\u00eda\n    necesaria y sincronizaci\u00f3n con EMHASS. Cumple con las reglas de Home Assistant\n    2026 para runtime_data y tipado estricto.\n    \"\"\"\n\n    def __init__(\n        self,\n        hass: HomeAssistant,\n        vehicle_id: str,\n        entry_id: Optional[str] = None,\n        presence_config: Optional[Dict[str, Any]] = None,\n        storage: Optional[YamlTripStorage] = None,\n        emhass_adapter: Optional[EMHASSAdapter] = None,\n    ) -> None:\n        \"\"\"Inicializa el gestor de viajes para un veh\u00edculo espec\u00edfico.\"\"\"\n        global _trip_manager_instance_count\n        _trip_manager_instance_count += 1\n        s\n\n--------------------------------------------------\nClass #17: TripPlannerCoordinator\nFile: custom_components/ev_trip_planner/coordinator.py:35\nBase classes: DataUpdateCoordinator\nPublic methods: 2\n\n  [private] __init__(arity=6, +return)\n  [PUBLIC] vehicle_id(arity=1, +return)\n  [private] _async_update_data(arity=1, +return)\n  [PUBLIC] async_refresh_trips(arity=1, +return)\n  [private] _generate_mock_emhass_params(arity=2, +return)\n\nSource preview:\nclass TripPlannerCoordinator(DataUpdateCoordinator):\n    \"\"\"Coordinator for EV Trip Planner data updates.\n\n    This coordinator holds the canonical view of all EV Trip Planner data,\n    reading from TripManager on each refresh cycle and exposing it via\n    coordinator.data for all sensors to consume via CoordinatorEntity pattern.\n\n    Data contract (Phase 1 - EMHASS keys as None):\n        {\n            \"recurring_trips\": dict of trip_id -> trip_data,\n            \"punctual_trips\": dict of trip_id -> trip_data,\n            \"kwh_today\": float,\n            \"hours_today\": float,\n            \"next_trip\": dict or None,\n            \"emhass_power_profile\": None,      # populated in Phase 3\n            \"emhass_deferrables_schedule\": None,  # populated in Phase 3\n            \"emhass_status\": None,   \n\n--------------------------------------------------\nClass #18: RetryState\nFile: custom_components/ev_trip_planner/vehicle_controller.py:25\nBase classes: None\nPublic methods: 4\n\n  [PUBLIC] add_attempt(arity=1, +return)\n  [PUBLIC] should_retry(arity=1, +return)\n  [PUBLIC] get_attempt_count(arity=1, +return)\n  [PUBLIC] reset(arity=1, +return)\n\nSource preview:\nclass RetryState:\n    \"\"\"Tracks retry attempts for charging activation.\"\"\"\n\n    attempts: List[float] = field(default_factory=list)\n\n    def add_attempt(self) -> None:\n        \"\"\"Add a timestamp for this retry attempt.\"\"\"\n        current_time = time.time()\n        self.attempts.append(current_time)\n        # Clean up old attempts outside the time window\n        self.attempts = [\n            t for t in self.attempts if current_time - t <= RETRY_TIME_WINDOW_SECONDS\n        ]\n\n    def should_retry(self) -> bool:\n        \"\"\"Check if we should allow another retry attempt.\n\n        Returns:\n            True if attempts are within the limit, False if exceeded.\n        \"\"\"\n        current_time = time.time()\n        # Clean up old attempts first\n        self.attempts = [\n            t for t in self\n\n--------------------------------------------------\nClass #19: HomeAssistantWrapper\nFile: custom_components/ev_trip_planner/vehicle_controller.py:65\nBase classes: None\nPublic methods: 2\n\n  [private] __init__(arity=2, NO-return)\n  [PUBLIC] async_call_service(arity=4, +return)\n  [PUBLIC] get_state(arity=2, NO-return)\n\nSource preview:\nclass HomeAssistantWrapper:\n    \"\"\"Wrapper for Home Assistant service and state calls to enable testing.\"\"\"\n\n    def __init__(self, hass: HomeAssistant):\n        \"\"\"Initialize wrapper.\"\"\"\n        self._hass = hass\n\n    async def async_call_service(\n        self, domain: str, service: str, data: Dict[str, Any]\n    ) -> (\n        None\n    ):  # pragma: no cover  # HA service I/O - direct service call delegation to HA\n        \"\"\"Call a service.\"\"\"\n        await self._hass.services.async_call(domain, service, data)\n\n    def get_state(self, entity_id: str):\n        \"\"\"Get state of an entity.\"\"\"\n        return self._hass.states.get(entity_id)\n\n--------------------------------------------------\nClass #20: VehicleControlStrategy\nFile: custom_components/ev_trip_planner/vehicle_controller.py:85\nBase classes: ABC\nPublic methods: 3\n\n  [private] __init__(arity=3, NO-return)\n  [PUBLIC] async_activate(arity=1, +return)\n  [PUBLIC] async_deactivate(arity=1, +return)\n  [PUBLIC] async_get_status(arity=1, +return)\n\nSource preview:\nclass VehicleControlStrategy(ABC):\n    \"\"\"Abstract base class for vehicle control strategies.\"\"\"\n\n    def __init__(self, hass_wrapper: HomeAssistantWrapper, config: Dict[str, Any]):\n        \"\"\"Initialize strategy.\"\"\"\n        self.hass_wrapper = hass_wrapper\n        self.config = config\n\n    @abstractmethod\n    async def async_activate(self) -> bool:\n        \"\"\"Activate vehicle charging.\"\"\"\n        pass\n\n    @abstractmethod\n    async def async_deactivate(self) -> bool:\n        \"\"\"Deactivate vehicle charging.\"\"\"\n        pass\n\n    @abstractmethod\n    async def async_get_status(self) -> bool:\n        \"\"\"Get current charging status.\"\"\"\n        pass\n\n--------------------------------------------------\nClass #21: SwitchStrategy\nFile: custom_components/ev_trip_planner/vehicle_controller.py:109\nBase classes: VehicleControlStrategy\nPublic methods: 3\n\n  [private] __init__(arity=3, NO-return)\n  [PUBLIC] async_activate(arity=1, +return)\n  [PUBLIC] async_deactivate(arity=1, +return)\n  [PUBLIC] async_get_status(arity=1, +return)\n\nSource preview:\nclass SwitchStrategy(VehicleControlStrategy):\n    \"\"\"Control via switch entity.\"\"\"\n\n    def __init__(self, hass_wrapper: HomeAssistantWrapper, config: Dict[str, Any]):\n        super().__init__(hass_wrapper, config)\n        self.switch_entity_id = config[\"entity_id\"]\n\n    async def async_activate(self) -> bool:\n        \"\"\"Turn on switch.\"\"\"\n        try:\n            await self.hass_wrapper.async_call_service(\n                \"switch\", \"turn_on\", {\"entity_id\": self.switch_entity_id}\n            )\n            _LOGGER.info(\"Activated charging via switch: %s\", self.switch_entity_id)\n            return True\n        except Exception as err:\n            _LOGGER.error(\"Error activating switch: %s\", err, exc_info=True)\n            return False\n\n    async def async_deactivate(self) -> bool:\n        \"\"\n\n--------------------------------------------------\nClass #22: ServiceStrategy\nFile: custom_components/ev_trip_planner/vehicle_controller.py:150\nBase classes: VehicleControlStrategy\nPublic methods: 3\n\n  [private] __init__(arity=3, NO-return)\n  [PUBLIC] async_activate(arity=1, +return)\n  [PUBLIC] async_deactivate(arity=1, +return)\n  [PUBLIC] async_get_status(arity=1, +return)\n\nSource preview:\nclass ServiceStrategy(VehicleControlStrategy):\n    \"\"\"Control via custom service call.\"\"\"\n\n    def __init__(self, hass_wrapper: HomeAssistantWrapper, config: Dict[str, Any]):\n        super().__init__(hass_wrapper, config)\n        self.service_on = config[\"service_on\"]\n        self.service_off = config[\"service_off\"]\n        self.data_on = config.get(\"data_on\", {})\n        self.data_off = config.get(\"data_off\", {})\n\n    async def async_activate(self) -> bool:\n        \"\"\"Call service to start charging.\"\"\"\n        try:\n            domain, service = self.service_on.split(\".\", 1)\n            await self.hass_wrapper.async_call_service(domain, service, self.data_on)\n            _LOGGER.info(\"Activated charging via service: %s\", self.service_on)\n            return True\n        except Exception as \n\n--------------------------------------------------\nClass #23: ScriptStrategy\nFile: custom_components/ev_trip_planner/vehicle_controller.py:195\nBase classes: VehicleControlStrategy\nPublic methods: 3\n\n  [private] __init__(arity=3, NO-return)\n  [PUBLIC] async_activate(arity=1, +return)\n  [PUBLIC] async_deactivate(arity=1, +return)\n  [PUBLIC] async_get_status(arity=1, +return)\n\nSource preview:\nclass ScriptStrategy(VehicleControlStrategy):\n    \"\"\"Control via script execution.\"\"\"\n\n    def __init__(self, hass_wrapper: HomeAssistantWrapper, config: Dict[str, Any]):\n        super().__init__(hass_wrapper, config)\n        self.script_on = config[\"script_on\"]\n        self.script_off = config[\"script_off\"]\n\n    async def async_activate(self) -> bool:\n        \"\"\"Execute start charging script.\"\"\"\n        try:\n            await self.hass_wrapper.async_call_service(\n                \"script\", self.script_on.replace(\"script.\", \"\"), {}\n            )\n            _LOGGER.info(\"Activated charging via script: %s\", self.script_on)\n            return True\n        except Exception as err:\n            _LOGGER.error(\n                \"Error executing script %s: %s\", self.script_on, err, exc_info=True\n   \n\n--------------------------------------------------\nClass #24: ExternalStrategy\nFile: custom_components/ev_trip_planner/vehicle_controller.py:238\nBase classes: VehicleControlStrategy\nPublic methods: 3\n\n  [PUBLIC] async_activate(arity=1, +return)\n  [PUBLIC] async_deactivate(arity=1, +return)\n  [PUBLIC] async_get_status(arity=1, +return)\n\nSource preview:\nclass ExternalStrategy(VehicleControlStrategy):\n    \"\"\"No direct control - external system manages charging.\"\"\"\n\n    async def async_activate(self) -> bool:\n        \"\"\"No-op.\"\"\"\n        _LOGGER.info(\"External strategy: no action taken\")\n        return True\n\n    async def async_deactivate(self) -> bool:\n        \"\"\"No-op.\"\"\"\n        _LOGGER.info(\"External strategy: no action taken\")\n        return True\n\n    async def async_get_status(self) -> bool:\n        \"\"\"Unknown status.\"\"\"\n        return False\n\n--------------------------------------------------\nClass #25: VehicleController\nFile: custom_components/ev_trip_planner/vehicle_controller.py:288\nBase classes: None\nPublic methods: 9\n\n  [private] __init__(arity=5, +return)\n  [PUBLIC] async_setup(arity=1, +return)\n  [PUBLIC] set_strategy(arity=2, +return)\n  [PUBLIC] update_config(arity=2, +return)\n  [PUBLIC] async_check_presence_status(arity=1, +return)\n  [private] _async_check_charging_sensor(arity=1, +return)\n  [PUBLIC] async_activate_charging(arity=1, +return)\n  [private] _check_and_reset_retry_on_disconnect(arity=1, +return)\n  [PUBLIC] reset_retry_state(arity=1, +return)\n  [PUBLIC] get_retry_state(arity=1, +return)\n  [PUBLIC] async_deactivate_charging(arity=1, +return)\n  [private] _update_charging_state_after_deactivation(arity=1, +return)\n  [PUBLIC] async_get_charging_status(arity=1, +return)\n\nSource preview:\nclass VehicleController:\n    \"\"\"Vehicle controller for managing charging control strategies.\n\n    This class provides an interface for controlling vehicle charging using\n    different strategies (switch, service, script, or external).\n    \"\"\"\n\n    def __init__(\n        self,\n        hass: HomeAssistant,\n        vehicle_id: str,\n        presence_config: Optional[Dict[str, Any]] = None,\n        trip_manager: Optional[\"TripManager\"] = None,\n    ) -> None:\n        \"\"\"Initialize the vehicle controller.\"\"\"\n        self.hass = hass\n        self.vehicle_id = vehicle_id\n        self._strategy: Optional[VehicleControlStrategy] = None\n        self._config: Dict[str, Any] = {}\n        self._presence_monitor: Optional[PresenceMonitor] = None\n        if presence_config:\n            self._charging_sensor\n\n--------------------------------------------------\nClass #26: TripSensorEntityDescription\nFile: custom_components/ev_trip_planner/definitions.py:35\nBase classes: SensorEntityDescription\nPublic methods: 0\n\n\nSource preview:\nclass TripSensorEntityDescription(SensorEntityDescription):\n    \"\"\"Entity description for trip sensors.\n\n    Extends SensorEntityDescription with\n    custom fields for trip data processing.\n    \"\"\"\n\n    value_fn: Callable[[dict[str, Any]], Any] = field(\n        default_factory=lambda: lambda data: None\n    )\n    attrs_fn: Callable[[dict[str, Any]], dict[str, Any]] = field(\n        default_factory=lambda: default_attrs_fn\n    )\n    restore: bool = False\n    exists_fn: Callable[[dict[str, Any]], bool] = field(\n        default_factory=lambda: lambda _: True\n    )\n\n--------------------------------------------------\nClass #27: YamlTripStorage\nFile: custom_components/ev_trip_planner/yaml_trip_storage.py:12\nBase classes: None\nPublic methods: 2\n\n  [private] __init__(arity=3, +return)\n  [PUBLIC] async_load(arity=1, +return)\n  [PUBLIC] async_save(arity=2, +return)\n\nSource preview:\nclass YamlTripStorage:\n    \"\"\"YAML-based storage for trips using Home Assistant's Store API.\n\n    This storage backend uses HA's Store API which persists data as JSON\n    in the .storage directory. The YAML name is retained for historical\n    compatibility with the original design intent.\n    \"\"\"\n\n    def __init__(self, hass: HomeAssistant, vehicle_id: str) -> None:\n        \"\"\"Initialize storage for a vehicle.\n\n        Args:\n            hass: HomeAssistant instance\n            vehicle_id: Vehicle identifier\n        \"\"\"\n        self._hass = hass\n        self._vehicle_id = vehicle_id\n        storage_key = f\"{DOMAIN}_{vehicle_id}\"\n\n        self._store: ha_storage.Store[dict[str, Any]] = ha_storage.Store(\n            hass, version=1, key=storage_key\n        )\n\n    async def async_load(self) ->\n\n--------------------------------------------------\nClass #28: EVTripRuntimeData\nFile: custom_components/ev_trip_planner/__init__.py:52\nBase classes: None\nPublic methods: 0\n\n\nSource preview:\nclass EVTripRuntimeData:\n    \"\"\"Runtime data container for a single vehicle config entry.\"\"\"\n\n    coordinator: Any\n    trip_manager: TripManager | None = None\n    sensor_async_add_entities: (\n        Callable[[list[SensorEntity], bool], Awaitable[None]] | None\n    ) = None\n    emhass_adapter: Any = None\n    # T3.1: Timer handle for hourly refresh \u2014 must be cancelled on unload to prevent EC-001 leak\n    hourly_refresh_cancel: Callable[[], None] | None = None\n\n--------------------------------------------------\nClass #29: EVTripPlannerFlowHandler\nFile: custom_components/ev_trip_planner/config_flow.py:287\nBase classes: ConfigFlow\nPublic methods: 7\n\n  [PUBLIC] async_migrate_entry(arity=2, +return)\n  [private] __init__(arity=1, +return)\n  [private] _get_vehicle_data(arity=1, +return)\n  [PUBLIC] async_step_user(arity=2, +return)\n  [PUBLIC] async_step_sensors(arity=2, +return)\n  [PUBLIC] async_step_emhass(arity=2, +return)\n  [PUBLIC] async_step_presence(arity=2, +return)\n  [PUBLIC] async_step_notifications(arity=2, +return)\n  [private] _async_create_entry(arity=1, +return)\n  [PUBLIC] async_get_options_flow(arity=1, +return)\n\nSource preview:\nclass EVTripPlannerFlowHandler(config_entries.ConfigFlow):\n    \"\"\"Maneja el flujo de configuraci\u00f3n para EV Trip Planner.\"\"\"\n\n    VERSION = CONFIG_VERSION\n    CONNECTION_CLASS = config_entries.CONN_CLASS_LOCAL_PUSH\n\n    @staticmethod\n    async def async_migrate_entry(hass: Any, entry: config_entries.ConfigEntry) -> bool:\n        \"\"\"Migrar entrada de configuraci\u00f3n de versi\u00f3n anterior.\n\n        v2 -> v3: Add battery health config (t_base, soh_sensor).\n        \"\"\"\n        _LOGGER.info(\n            \"Migrating config entry from version %s to %s\",\n            entry.version,\n            CONFIG_VERSION,\n        )\n\n        if entry.version == 2:\n            # Add new v3 fields with safe defaults\n            new_data = dict(entry.data)\n            new_data[CONF_T_BASE] = DEFAULT_T_BASE\n            ne\n\n--------------------------------------------------\nClass #30: EVTripPlannerOptionsFlowHandler\nFile: custom_components/ev_trip_planner/config_flow.py:936\nBase classes: OptionsFlow\nPublic methods: 1\n\n  [private] __init__(arity=2, +return)\n  [PUBLIC] async_step_init(arity=2, +return)\n\nSource preview:\nclass EVTripPlannerOptionsFlowHandler(config_entries.OptionsFlow):\n    \"\"\"Maneja las opciones de configuraci\u00f3n para EV Trip Planner.\"\"\"\n\n    def __init__(self, config_entry: config_entries.ConfigEntry) -> None:\n        \"\"\"Inicializa el flujo de opciones.\"\"\"\n        self._config_entry = config_entry\n\n    async def async_step_init(\n        self, user_input: Optional[Dict[str, Any]] = None\n    ) -> FlowResult:\n        \"\"\"Paso inicial del flujo de opciones.\"\"\"\n        _LOGGER.debug(\"Options flow step init: showing form\")\n\n        if user_input is not None:\n            _LOGGER.debug(\n                \"Options flow step init: battery=%.1f, charging=%.1f, \"\n                \"consumption=%.2f, safety=%d, t_base=%.1f\",\n                user_input.get(CONF_BATTERY_CAPACITY, 0),\n                user_inp\n\n\n======================================================================\nSOLID EVALUATION CRITERIA\n======================================================================\n\nS - Single Responsibility: Does each class have ONE reason to change?\n    Red flags: >7 public methods, handles multiple concerns\n\nO - Open/Closed: Can behavior be extended without modifying source?\n    Red flags: no ABC/Protocol, no inheritance hierarchy\n\nL - Liskov Substitution: Can subclasses replace parents transparently?\n    Red flags: narrowed return types, strengthened preconditions\n\nI - Interface Segregation: Are interfaces small and focused?\n    Red flags: fat interfaces, unused methods\n\nD - Dependency Inversion: Do high-level modules depend on abstractions?\n    Red flags: concrete imports, no dependency injection\n\n======================================================================",
  "classes_with_many_methods": [
    {
      "name": "EMHASSAdapter",
      "file": "custom_components/ev_trip_planner/emhass_adapter.py",
      "public_methods": 27
    },
    {
      "name": "PresenceMonitor",
      "file": "custom_components/ev_trip_planner/presence_monitor.py",
      "public_methods": 11
    },
    {
      "name": "TripManager",
      "file": "custom_components/ev_trip_planner/trip_manager.py",
      "public_methods": 31
    },
    {
      "name": "VehicleController",
      "file": "custom_components/ev_trip_planner/vehicle_controller.py",
      "public_methods": 9
    }
  ],
  "classes_without_bases": [
    {
      "name": "EMHASSAdapter",
      "file": "custom_components/ev_trip_planner/emhass_adapter.py"
    },
    {
      "name": "PresenceMonitor",
      "file": "custom_components/ev_trip_planner/presence_monitor.py"
    },
    {
      "name": "DashboardImportResult",
      "file": "custom_components/ev_trip_planner/dashboard.py"
    },
    {
      "name": "BatteryCapacity",
      "file": "custom_components/ev_trip_planner/calculations.py"
    },
    {
      "name": "ChargingDecision",
      "file": "custom_components/ev_trip_planner/calculations.py"
    },
    {
      "name": "TripManager",
      "file": "custom_components/ev_trip_planner/trip_manager.py"
    },
    {
      "name": "RetryState",
      "file": "custom_components/ev_trip_planner/vehicle_controller.py"
    },
    {
      "name": "HomeAssistantWrapper",
      "file": "custom_components/ev_trip_planner/vehicle_controller.py"
    },
    {
      "name": "VehicleController",
      "file": "custom_components/ev_trip_planner/vehicle_controller.py"
    },
    {
      "name": "YamlTripStorage",
      "file": "custom_components/ev_trip_planner/yaml_trip_storage.py"
    },
    {
      "name": "EVTripRuntimeData",
      "file": "custom_components/ev_trip_planner/__init__.py"
    }
  ]
}
