---
spec: emhass-sensor-enhancement
basePath: specs/emhass-sensor-enhancement
phase: tasks
task: 3.3/3.5
updated: 2026-03-30
---

# Progress: emhass-sensor-enhancement

## Original Goal

Como sistema, necesito que `sensor.emhass_perfil_diferible_{vehicle_id}` tenga el formato correcto con indices `p_deferrable{n}` estables y se actualice automaticamente.

## Context (from epic)

**Epic**: emhass-deferrable-integration (4th of 7 specs)
**Previous specs completed**: soc-integration-baseline, charging-window-calculation, soc-milestone-algorithm

**Dependencies**: soc-milestone-algorithm (completed)

**Plan summary**: EMHASS sensor enhancement with stable indices (soft delete) and automatic updates.

## Acceptance Criteria

1. AC-1: 3 trips → p_deferrable0, p_deferrable1, p_deferrable2 with correct values
2. AC-2: Trip deletion → soft delete indices (marked "released" with timestamp, NOT reused immediately)
3. AC-3: SOC change while home+plugged → sensor updates with new power_profile_watts
4. AC-4: deferrables_schedule shows p_deferrable{n} for specific hour
5. AC-5: EMHASS MPC plan → automation can read p_deferrable{n} per hour

## Notes

- Index stability via soft delete with 24h cooldown
- Released indices stored in `_released_indices` dict with timestamp
- No Available Tools section needed (using ralph-specum for verification)

## Completed Tasks

- [x] 5.1 Monitor CI pipeline - (CI checks passed: 2/2 tests green)
- [x] 5.2 Address code review comments - (fixed kill_pytest_orphans.py psutil fallback, fixed test_production_errors.py missing assertion)
- [x] 4.1 Local quality check - (lint fix: unused imports in test_emhass_soft_delete.py)
- [x] 4.2 Create PR and verify CI - 3bcb626
- [x] 3.4 Add test for multiple trips with correct p_deferrable indices - 69a2d96
- [x] 3.3 Add integration test for deferrables_schedule p_deferrable{n} per hour - a1413b0
- [x] 3.1 Add unit tests for soft delete index stability - 4f9de5a
- [x] 3.2 Add tests for sensor last_update and emhass_status attributes -
- [x] 1.1 Add last_update and emhass_status attributes to EmhassDeferrableLoadSensor - 50d70f3
- [x] 1.2 Add soft delete mechanism to EMHASSAdapter - 0f8a2e1
- [x] 1.3 Add get_available_indices method with cooldown logic - pre-implemented
- [x] 1.4 Update async_update in sensor to include trips_count and vehicle_id - 73d081b
- [x] 1.5 [VERIFY] Quality checkpoint: POC builds and basic functionality works - PASS
- [x] 1.6 POC Checkpoint: Verify sensor has correct attributes format - 2 passed
- [x] 2.1 Extract EMHASS status constants - (extracted constants for EMHASS_STATE_READY, EMHASS_STATE_ACTIVE, EMHASS_STATE_ERROR)
- [x] 2.2 Add index cooldown configuration to config flow - (added CONF_INDEX_COOLDOWN_HOURS with default 24h, wired to EMHASSAdapter)

## Learnings

- Task planning insight: EMHASSAdapter manages index lifecycle (soft delete with _released_indices dict), sensor just displays data via extra_state_attributes
- Dependency discovered: EmhassDeferrableLoadSensor.async_update() calls trip_manager.async_generate_power_profile() and async_generate_deferrables_schedule() - these must exist
- Index stability issue: current emhass_adapter.py releases indices immediately to _available_indices (line 127), needs soft delete implementation
- Verification commands: `make test`, `make lint`, `make mypy` from Makefile at project root
- Test file location: tests/test_emhass_adapter.py for index tests, tests/test_deferrable_load_sensors.py for sensor tests
- POC-First workflow selected because this is GREENFIELD (new enhancement building on completed specs)
- Quality checkpoints inserted every 2-3 tasks per phase rules
- get_available_indices() was already implemented in the codebase (likely as part of task 1.2 or earlier iteration)

### Verification: 1.5 [VERIFY] Quality checkpoint
- Status: PASS
- Command: ruff check custom_components/ev_trip_planner/sensor.py custom_components/ev_trip_planner/emhass_adapter.py
- Result: All checks passed (no lint errors)
- Code compiles: Yes (Python syntax valid)

### Verification: 2.2 Add index cooldown configuration
- Status: PASS
- grep -n "cooldown" shows cooldown field in config_flow.py (field added to STEP_EMHASS_SCHEMA with range 1-168) and emhass_adapter.py (reads from vehicle_config)
- ruff check: All checks passed

### Verification: 3.3 Add integration test for deferrables_schedule p_deferrable{n} per hour
- Status: PASS
- Command: python3 -m pytest tests/test_deferrables_schedule.py -v --tb=short -k "p_deferrable" 2>&1 | tail -20
- Result: 3 passed (test_schedule_shows_p_deferrable_per_hour + 2 existing tests)
- Commit: a1413b0

### Verification: 3.5 [VERIFY] Quality checkpoint: all tests pass
- Status: PASS
- Command: make test
- Result: 839 passed, 10 warnings in 18.14s
- Coverage: 81.78% (required 79%)
- Fixes applied: Updated test_release_trip_index and test_remove_deferrable_load to reflect soft delete behavior (indices go to _released_indices instead of immediately returning to _available_indices)
