---
spec: emhass-sensor-entity-lifecycle
basePath: specs/emhass-sensor-entity-lifecycle
phase: research
updated: 2026-04-05T00:00:00Z
---

# Progress: emhass-sensor-entity-lifecycle

## Goal

Fix the EMHASS sensor entity (`sensor.ev_trip_planner_{vehicle}_emhass_perfil_diferible_{entry_id}`) lifecycle issues:

1. Sensors not deleted when vehicle is deleted
2. Sensors showing on other vehicles' panels
3. Charging power changes in config flow not reflected in sensor attributes
4. `power_profile_watts` not updating when vehicle params change (only `deferrables_schedule` updates)

## Context Audit

**Audited**: 2026-04-05
**Total assertions found**: 4
**Status**: CLEAN

### Filesystem
- ✅ `custom_components/` — exists
- ✅ `tests/` — exists
- ✅ `specs/` — exists
- ✅ `.venv/` — exists

### Commands
- ✅ `pytest` in .venv — available

### Environment Files
- ✅ No .env assertions found in system prompt (safe — not exposing secrets)

### Scripts
- ✅ pytest available in .venv/bin/

## Skill Discovery
- **context-auditor** (plugin): always-invoked (reason: mandatory system prompt validation)

## Research Completion

**Completed**: 2026-04-05
**Findings added**:
- Panel deletion failure: panel cleanup guarded by `if unload_ok:` - skipped if platform unload fails
- Test gap analysis: tests mock `hass.states` but never verify `entity_registry` cleanup
- 5 specific test gaps identified with file:line references

## Research Summary

**Issues investigated**:
1. ✅ Dual entity creation (state-only vs entity registry)
2. ✅ Sensor deletion failure
3. ✅ Cross-vehicle sensor contamination
4. ✅ Charging power not reflected in sensor
5. ✅ `power_profile_watts` not updating
6. ✅ **Panel sidebar link persists after vehicle deletion** (new - added from user feedback)
7. ✅ **Why tests didn't catch these bugs** (new - added from user feedback)

**Status**: Requirements complete - awaiting user approval to proceed to design
