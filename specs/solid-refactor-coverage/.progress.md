# solid-refactor-coverage

## Context Audit

**Audited**: 2026-04-08
**Total assertions found**: 3
**Status**: CLEAN

### Filesystem
- ✅ `.github/copilot-instructions.md` — exists
- ✅ `docs/CODEGUIDELINESia.md` — exists
- ✅ `Makefile` — exists

### Scripts
- ✅ `make e2e` — defined in Makefile (needs manual verification of target)

### Environment Files
- No verifiable .env assertions found in system prompt

---

## Intent Classification
- Type: REFACTOR
- Confidence: high (multiple keywords: refactorizar, SOLID, cobertura)
- Min questions: 3
- Max questions: 5
- Keywords matched: [refactorizar, SOLID, cobertura]

## Goal

Refactorizar el código fuente aplicando principios SOLID de forma que los tests sean fáciles de escribir usando el doble apropiado según la tabla. La cobertura 100% debe ser consecuencia del buen diseño, no el objetivo directo.

Donde ya existan tests que usen `MagicMock()` sin `spec`, o Patches innecesariamente profundos, o Fakes/Stubs definidos en `conftest.py` en vez de `tests/__init__.py` — refactorizarlos también para alinearlos con la Layered Test Doubles Strategy. Solo donde merezca la pena: si el test pasa y el doble es correcto para su capa, no tocarlo.

## Skill Discovery
- **context-auditor** (plugin): always-invoked (reason: mandatory system prompt validation)

## Interview Responses

### Goal Interview
- **Problem being solved**: trip_manager.py (~1346L) y emhass_adapter.py (~1536L) son God Classes con lógica de negocio mezclada con I/O. 100% coverage es imposible sin doubles profundamente anidados.
- **Constraints and must-haves**: TripManager y EMHASSAdapter deben mantener backward compatibility en interfaz pública. Storage format no puede cambiar. Tests existentes deben pasar.
- **Success criteria**: coverage 100% como consecuencia del buen diseño; tests usan doubles correctos según Layered Test Doubles Strategy
- **Scope boundaries**: IN — pure functions extraction, Protocol DI, Layer 1 migration; OUT — split en subclases nuevas (no es el objetivo)
- **User's existing knowledge**: Ya sabe que MagicMock() sin spec es violación, que Fakes van en tests/__init__.py, que Layer 3 en conftest.py es solo fixtures

### Approach Selection
- **Chosen approach**: A → B → C → D incremental con Protocol DI
  - Fase A: Pure functions → calculations.py / utils.py
  - Fase B: TripStorageProtocol + EMHASSPublisherProtocol → protocols.py
  - Fase C: Inject via constructor, default real impl
  - Fase D: Migrate Layer 1 + fix MagicMock() violations incrementally
- **Rationale**: Funciones puras primero (fácil 100% cover, sin doubles), luego Protocols (DI sin reescribir clases), luego cleanup tests

### Goal Interview Answers
1. Phase order: **A - Pure functions first** (Recommended) ✅
2. TripManager scope: **B - Protocols + DI** — extract pure funcs + inject Protocols via constructor, NO full SRP split ❌
3. Test doubles migration: **C - Migrate incrementally** — migrate as each source module refactored ✅
4. MagicMock fixes: **A - Fix incrementally** — fix files being refactored during Phase A-D ✅

## Interview Format
- Version: 1.0
- Intent: REFACTOR (high confidence, 3+ keywords)
- Min questions: 3 / Max questions: 5

## T001: validate_hora failing tests
- **Completed**: 2026-04-08
- **Status**: RED phase complete - tests written and failing with ImportError
- **What was done**: Added TestValidateHora class to tests/test_utils.py with 14 test cases:
  - Valid HH:MM format passes (5 cases: 09:30, 00:00, 23:59, 12:00, 00:00)
  - Invalid hour > 23 (2 cases: 25:00, 24:00)
  - Invalid minute > 59 (2 cases: 12:60, 12:61)
  - Invalid format non-numeric (2 cases: "abc", "")
  - Invalid format incomplete (2 cases: "9:30", "0930")
- **Failure mode**: ImportError - validate_hora not yet implemented in utils.py
- **Commit**: 38b6e7d "test(phase-a): red - failing test for validate_hora"

## T002: sanitize_recurring_trips failing tests
- **Completed**: 2026-04-08
- **Status**: RED phase complete - tests written and failing with ImportError
- **What was done**: Added TestSanitizeRecurringTrips class to tests/test_utils.py with 7 test cases:
  - Filters trips with invalid hora (25:00)
  - Keeps trips with valid hora (2 valid trips)
  - Filters invalid minute format (09:60)
  - Filters invalid format ("invalid")
  - Returns empty dict for all invalid
  - Returns empty dict for empty input
  - Preserves trip data for valid entries
- **Failure mode**: ImportError - sanitize_recurring_trips not yet implemented in utils.py
- **Commit**: 295c16d "test(phase-a): red - failing test for sanitize_recurring_trips"

## T003: is_trip_today failing tests
- **Completed**: 2026-04-08
- **Status**: RED phase complete - tests written and failing with ImportError
- **What was done**: Added TestIsTripToday class to tests/test_utils.py with 25 test cases:
  - Recurring trips (Spanish): lunes, martes, miercoles, jueves, viernes, sabado, domingo return True when day matches today
  - Recurring trips (English): monday through sunday return True when day matches today
  - Recurring trips return False when day does not match today
  - Recurring trips case insensitive matching
  - Punctual trips return True when date matches today
  - Punctual trips return False when date does not match today
  - Punctual trips handle date string formats: YYYYMMDD, ISO (YYYY-MM-DD), slash (YYYY/MM/DD)
  - Returns bool (not truthy/falsy value)
- **Failure mode**: ImportError - is_trip_today not yet implemented in utils.py
- **Commit**: e46c8a1 "feat(phase-a): green - implement is_trip_today"

## T008: is_trip_today implementation
- **Completed**: 2026-04-08
- **Status**: GREEN phase complete - implementation passes all 24 tests
- **What was done**: Added `is_trip_today(trip: dict, today: date) -> bool` to utils.py:
  - Recurring trips: maps `dia` (Spanish or English) to English day name via DAY_ABBREVIATIONS, compares with today.strftime("%A")
  - Punctual trips: handles date object comparison directly, string dates normalized to YYYYMMDD for comparison
  - Returns False for unknown trip types
- **Tests**: All 24 tests in TestIsTripToday pass
- **Commit**: pending

## T005: get_trip_time and get_day_index failing tests
- **Completed**: 2026-04-08
- **Status**: RED phase complete - tests written and failing with ImportError
- **What was done**: Added TestGetTripTime class (8 tests) and TestGetDayIndex class (20 tests) to tests/test_utils.py:
  - get_trip_time: extracts datetime from trip dict, returns None for missing/empty hora, handles boundary times 00:00 and 23:59
  - get_day_index: converts day names to indices (lunes/monday=0 through domingo/sunday=6), case insensitive, raises ValueError for invalid names
- **Failure mode**: ImportError - get_trip_time and get_day_index not yet implemented in utils.py
- **Commit**: 1c802c5 "test(phase-a): red - failing test for get_trip_time and get_day_index"

## T004: calculate_charging_rate and calculate_soc_target failing tests
- **Completed**: 2026-04-08
- **Status**: RED phase complete - tests written and failing (assertion errors, not ImportError)
- **What was done**: Added TestCalculateChargingRateT004 (5 tests) and TestCalculateSocTargetT004 (12 tests) to tests/test_calculations.py:
  - calculate_charging_rate(power_kw, capacity): returns power_kw pass-through, 0 capacity returns 0.0
  - calculate_soc_target(trip, capacity, consumption): formula (trip_distance * consumption) / capacity * 100, edge cases for zero capacity and consumption
- **Failure mode**: AssertionError - existing implementations have different signatures/behavior:
  - Existing `calculate_charging_rate` uses formula (power / capacity * 100) not pass-through
  - Existing `calculate_soc_target` doesn't take `consumption` parameter
- **Commit**: 526f375 "test(phase-a): red - failing test for calculate_charging_rate and calculate_soc_target"
- **Key insight**: tests/test_calculations.py already had tests for these functions with different signatures. T004 adds new test classes (T004 suffix) defining the desired Phase A interface. This creates parallel RED-phase test suites until T009/T010 implement the new signatures.

## Learnings

- **Phase ordering matters**: Extracting pure functions (Phase A) first is the right approach — it provides immediate test coverage wins without introducing DI complexity
- **Protocol DI is non-invasive**: Using `typing.Protocol` allows adding DI without modifying existing classes — TripManager and EMHASSAdapter don't need to inherit from anything
- **MagicMock() without spec is pervasive**: Over 250 instances found across test files — the violations are concentrated in test_emhass_adapter.py, test_dashboard.py, and test_trip_manager.py
- **tests/__init__.py is empty but documented**: The TDD_METHODOLOGY.md already contains the exact pattern that should be in tests/__init__.py — the content just needs to be created
- **calculations.py already has some pure functions**: `calculate_day_index`, `calculate_trip_time`, `calculate_charging_rate`, `calculate_soc_target` etc. are already extracted — Phase A should verify which TripManager/emhass_adapter functions are truly new extractions vs already done
- **Backward compatibility constraint is critical**: TripManager already has `set_emhass_adapter()` / `get_emhass_adapter()` — constructor injection must preserve this pattern
- **HA integration test complexity**: This is a `fullstack` project (HA integration with EMHASS HTTP API calls) — test doubles strategy must account for both the HA event bus and external HTTP dependencies
- **Testing Discovery Checklist passed**: pytest runner confirmed working via smoke test on test_trip_manager_core.py (47 tests pass). Coverage command: `pytest --cov=custom_components.ev_trip_planner`. ruff/mypy configured in pyproject.toml
- **utils.py already has trip_id generation functions**: `generate_trip_id()`, `is_valid_trip_id()`, `calcular_energia_kwh()` already exist in utils.py — Phase A for utils.py focuses on time validation/sanitization functions
- **calculations.py already has core SOC/window functions**: `calculate_day_index`, `calculate_trip_time`, `calculate_charging_rate`, `calculate_soc_target`, `calculate_energy_needed`, `calculate_charging_window_pure`, `calculate_multi_trip_charging_windows`, `calculate_soc_at_trip_starts`, `calculate_deficit_propagation`, `calculate_power_profile` all exist — EMHASSAdapter pure functions need to be added
- **AC-C1.1 constraint is strict**: Constructor takes `storage: TripStorageProtocol` NOT `| None = None` — default created inline as `storage if storage is not None else YamlTripStorage(...)` without if-branch in constructor body
- **Existing calculate_charging_rate/soc_target have different signatures**: The already-extant functions in calculations.py use different interfaces than Phase A specifies (e.g., soc_target takes (trip, capacity) not (trip, capacity, consumption)). T004 creates parallel test classes defining the Phase A interface. Tests fail with AssertionError rather than ImportError because the functions exist but with wrong signatures.
