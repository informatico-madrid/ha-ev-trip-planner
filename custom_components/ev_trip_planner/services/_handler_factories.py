"""Handler factory closures — re-exported from services.py (transitional).

Currently empty: inner handler functions close over `hass`/`entry` and
cannot be meaningfully extracted as independent factories.
"""

from __future__ import annotations
